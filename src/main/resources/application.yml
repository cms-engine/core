spring:
  application:
    name: engine
  datasource:
    url: jdbc:postgresql://localhost:5432/engine
    username: postgres
    password: 159753
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: update
  data:
    rest:
      base-path: /api/v1/repo

vaadin:
  launch-browser: false

server:
  port: 8080